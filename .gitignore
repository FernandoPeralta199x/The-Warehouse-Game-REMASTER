# ─── Unity ─────────────────────────────────────────────────────
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/*
!Builds/.gitkeep
[Ll]ogs/
UserSettings/
MemoryCaptures/
Recordings/

# ─── Addressables / generated delivery output ─────────────────
# Settings, groups and labels under Assets/AddressableAssetsData stay versioned.
# Built bundles/catalog delivery output is generated and must not pollute Git.
ServerData/
AddressablesBuildTEP.json

# ─── IDE / editor ──────────────────────────────────────────────
.vs/
.idea/
.vscode/
*.csproj
*.sln
*.user
*.tmp
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db
sysinfo.txt
crashlytics-build.properties

# ─── Proibido — ROMs e dumps (Workflow §9 / AGENTS.md) ────────
roms/
*.bin
*.smd
*.gen
*.mdx
*.zip
*.7z
*.rar
*.iso

# ─── Proibido — conteúdo extraído do jogo original ────────────
extracted_maps/
original_levels/
sprites_original/
music_original/
sfx_original/
screenshots_original/
passwords_original.txt
shove_it_offsets.md

# ─── Godot legacy (branch godot-legacy preserva histórico) ────
.godot/
.import/
export.cfg
export_presets.cfg

# ─── Memória do agente / runtime LLM ──────────────────────────
ruvector.db
ruvector.db-journal
ruvector.db-wal
ruvector.db-shm
*.sqlite
*.sqlite3
.claude/
.cursor/

# ─── Sistema ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.log

# ─── REFERENCIA (design bible - deve entrar? ver decisão do owner) ──
# Descomentar a linha abaixo para EXCLUIR a pasta de referência do repo.
# Por padrão ela ENTRA (contém docs, sprites de referência, personagens).
# Cuidado: pode conter arquivos grandes (PNG grandes). Considere LFS.
# REFERENCIA/
